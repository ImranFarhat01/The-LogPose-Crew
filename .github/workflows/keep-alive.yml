name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the app
        run: curl -sS -o /dev/null -w "%{http_code}\n" "https://the-logpose-crew.streamlit.app/"
